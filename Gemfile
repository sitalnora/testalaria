# frozen_string_literal: true

source "https://rubygems.org"

# Gem dependencies are declared in the gemspec.
gemspec

group :development, :test do
  gem "rake", ">= 13.0"
  gem "rspec", "~> 3.12"
  gem "minitest", "~> 5.0"
  # prism is bundled with Ruby 3.3+; on older Rubies we fall back to Ripper.
end
